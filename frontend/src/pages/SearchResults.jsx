import ProductList from './ProductList'

function SearchResults() {
  // SearchResults uses the same component as ProductList
  // The search query is handled in ProductList component
  return <ProductList />
}

export default SearchResults
